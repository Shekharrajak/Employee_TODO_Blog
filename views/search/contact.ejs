<div class="container">
	<div class="row">
		<div class="col-sm-4 col-sm-offset-2 animated fadeInLeft">
			<form method="POST" action="/search/contact">
			
		</div>
		<div class="searchBar col-sm-4 animated fadeInRight">
				<input type="text" name="searchQuery" placeholder="Search: 'Web Designer'" class="form-control">
				<input type="submit" class="btn pull-right" value="Search">
			</form>
		</div>
	</div>
	<br>
	<div class="row">
		<div class="col-sm-8 col-sm-offset-2 posts animated fadeInUp">
			<h5 class="resultsCount"></h5>
			<% _.each(results, function(post) { %>
			<div class="post">
				<p class="Title"><a href="/post/show/<%= employee.name %>" data-job="<%= post.id %>"><%= employee.name %></a></p>
				<p><span class="company"><%= post.companyName %></span> <span class="location">(<%= employee.email %>)</span></p>
				<p class="oneLiner"><%= post.oneLiner %></p>
				<% if (req.session.authenticated && req.session.User.id == employee.userID) { %>
				<a href="/post/edit/<%= post.id %>">Edit Job</a>
				<% } %>
				<p class="createdAt pull-right" datetime="<%= post.createdAt %>"><%= post.createdAt %></p>
			</div>
			<% }) %>

			
		</div>
	</div>
</div>